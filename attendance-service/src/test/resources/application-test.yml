server:
  port: 8084

spring:
  application:
    name: attendance-service-test
  r2dbc:
    url: r2dbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:gym_administrator}
    username: ${DB_USER:administrador}
    password: ${DB_PASSWORD:seya1922}

jwt:
  secret: ${JWT_SECRET:Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24tdGhpcy1rZXktbXVzdC1iZS0yNTYtYml0cw==}

services:
  core-service:
    url: ${CORE_SERVICE_URL:http://localhost:8083}

scheduling:
  messaging-job-cron: "-"

app:
  cors:
    allow-all: true

logging:
  level:
    com.gymadmin.attendance: DEBUG
    io.r2dbc: INFO
